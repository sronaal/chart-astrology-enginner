[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "astrology-chart-engine"
version = "0.1.0"
description = "Deterministic natal chart calculation engine"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pysweph",
    "pydantic",
    "pydantic-settings",
    "uvicorn"
]

[project.optional-dependencies]
dev = [
    "httpx",
    "pytest"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]
