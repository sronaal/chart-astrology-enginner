services:
  db:
    image: postgres:16-alpine
    container_name: astrogyia-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: astrogyia
      POSTGRES_USER: astrogyia
      POSTGRES_PASSWORD: astrogyia
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U astrogyia"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
